name: ci
on: [push]
jobs:
  run_ci:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@master

    - name: Install dependencies
      run: make deps

    - name: Install dotfiles
      run: make dotfiles
