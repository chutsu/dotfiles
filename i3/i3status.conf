general {
  colors = true
  interval = 1
}

order += "battery 0"
order += "tztime local"

battery 0 {
  format = " %status %percentage"
  integer_battery_capacity = true
}

tztime local {
  format = " %Y-%m-%d  %H:%M "
}
